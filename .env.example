PORT=3000
HOST=0.0.0.0
MAX_CONCURRENT_RENDERS=4
RENDER_TIMEOUT_MS=60000
RETENTION_HOURS=24
WALLET_ADDRESS=
FACILITATOR_URL=https://x402.org/facilitator
NETWORK=eip155:84532

# Testnet payment harness (scripts/pay-and-generate.ts) — client side only.
# CLIENT_PRIVATE_KEY: a Base Sepolia test wallet funded with testnet USDC. NEVER commit a real key.
CLIENT_PRIVATE_KEY=
# SERVER_URL: where the running Clipcast server is reachable.
SERVER_URL=http://localhost:3000
# RPC_URL: optional Base Sepolia RPC override (defaults to viem's public endpoint).
RPC_URL=
